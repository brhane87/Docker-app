name: Docker app build

on: [push]

jobs: 
  build: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: docker build my app
        run: |
         docker build ./my-app -t myapp:1.0.0
    
